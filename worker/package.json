{
  "name": "tbes-redirect-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/entry.js && node --check src/short-redirects.js && node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
